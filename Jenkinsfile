pipeline {
    agent any

    stages {
        stage('Build Image') {
            steps {
                echo 'building application'
            }
        }

        stage('Test Image') {
            steps {
                echo 'Test application'
            }
        }

       stage('Deploy Image') {
            steps {
                echo 'Deploy application'
            }
        }
    }
}
